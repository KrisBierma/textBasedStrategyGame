
// #include "space.hpp"
// #include <map>

// string displayRoundMenu(int *infoArray, bool hasBackpack, Space *currentSpace, vector<string> &backpack, std::map<string, string> &droppedItemsMap, int numPossibleMoves, bool lightSwitchedOn, bool powerButtonPushed);

// string toString(int numIn);
